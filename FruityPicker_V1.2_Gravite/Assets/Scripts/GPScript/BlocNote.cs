using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlocNote : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    //    int iIdChercher = 0;



    //    for (int k = 0; k < 50; k++)
    //    {

    //        if (iSensPropagation == 1)
    //        {

    //            iIdChercher = iIdParent;

    //        }
    //        else if (iSensPropagation == 2)
    //        {

    //            iIdChercher

    //        }

    //    }

    //    for (int j = 0; j < 50; j++)
    //    {

    //        for (int i = 0; i < 50; i++)
    //        {

    //            int iIdVerif = hMainCam.GetComponent<MainGame>().mTableJunction[i].GetComponent<Jonction>().iIdActuel;

    //            if (iIdVerif == iIdChercher)
    //            {
    //                if (iEtat >= 0)
    //                {
    //                    hMainCam.GetComponent<MainGame>().mTableJunction[i].GetComponent<Jonction>().iEtat = hMainCam.GetComponent<MainGame>().mTableJunction[i].GetComponent<Jonction>().iEtat + 1;
    //                }
    //                else
    //                {

    //                    hMainCam.GetComponent<MainGame>().mTableJunction[i].GetComponent<Jonction>().iEtat = hMainCam.GetComponent<MainGame>().mTableJunction[i].GetComponent<Jonction>().iEtat - 1;

    //                }



    //                iIdChercher = hMainCam.GetComponent<MainGame>().mTableJunction[i].GetComponent<Jonction>().iIdParent;


    //            }
    //        }


    //        if (iIdParent == 0)
    //        {

    //            j = 100;

    //        }

    //    }
    }
}
